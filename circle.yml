machine:
  ruby:
    version: 2.2.2
  services:
    - docker

# test:
#   override:
#     - docker build -t thing/CIRCLE_BRANCH .
#     - docker run --name CIRCLE_BRANCH thing/CIRCLE_BRANCH .
#     - make acceptance
#
# deployment:
#   docker:
#     branch: CIRCLE_BRANCH
#     commands:
#       - ./build/build-docker-deploy.sh:
#          timeout: 600
